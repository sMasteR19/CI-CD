SOLID:
block1: 'OCP.Принцип открытости/закрытости'
# для расчета площади новой фигуры придется вводить дополнительное условие
block1: 'LSP.Принцип подстановки лисков'
# код не будет работать при подстановке подтипа. Также присутствуют различные проверки типов внутри функций 
block2: 'ISP.Принцип разделения интерфейса'
# класс worker используется в различных методах и свойствах, в которых не используется класс robot. Много свойств не заданы
# методы и свойства используются классом Worker, но какие-то из них не используются Robot 
block3: 'SRP.Принцип единственной ответственности'
# методы классы не связаны друг с другом
block4: 'DIP.Принцип инверсии зависимостей'
# нет зависимости на абстракциях, а есть на конкретном объекте. Объект LightBulb создаётся в методе Construction 
block5: 'ISP.Принцип разделения интерфейса, LSP.Принцип подстановки лисков'
# метод Fly не может использоваться классом Penguin, но может использоваться классом Bird
# при выполнении кода будет ошибка при подстановке в кода в подтип 
